import React from 'react';

const Bugs_and_fish_card = (props) => {
    return (
        // container
        <div>
            {/* a div to hold our filter bar. */}
            <div></div>
            {/* div to contain the results */}
            <div>
            {/*map over props and return a collectables (fish/bug) card*/}
            </div>
        </div>
    )
}

export default Bugs_and_fish_card;